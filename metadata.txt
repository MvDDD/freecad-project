; An example for this metadata.txt from
; http://docs.qgis.org/testing/en/docs/pyqgis_developer_cookbook/plugins.html#plugin-metadata
; the next section is mandatory

[general]
name=ProjectManager
email=mffvdaalen@gmail.com
author=mffvd
qgisMinimumVersion=2.0
description=This plugin handles versioning and external csv dimentions
about=
version=version 1.5
tracker=https://github.com/MvDDD/freecad-project/issues
repository=https://github.com/MvDDD/freecad-project
; end of mandatory metadata

; start of optional metadata
category=
changelog=

; Tags are in comma separated value format, spaces are allowed within the
; tag name.
; Tags should be in English language. Please also check for existing tags and
; synonyms before creating a new one.
tags=

; these metadata can be empty, they will eventually become mandatory.
homepage=https://github.com/
icon=

; experimental flag (applies to the single version)
experimental=True

; deprecated flag (applies to the whole plugin and not only to the uploaded version)
deprecated=False

; if empty, it will be automatically set to major version + .99
qgisMaximumVersion=

[dependencies]
pylibs=os,csv,yaml,subprocess,glob,ImportGui
